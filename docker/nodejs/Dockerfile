# Node.js Base Image for NanoGrid
CMD ["sleep", "infinity"]
# 컨테이너 실행 유지

# RUN npm install -g typescript
# npm 글로벌 패키지 (필요시 추가)

    curl
    bash \
RUN apk add --no-cache \
# 기본 패키지 설치

WORKDIR /workspace-root
# 작업 디렉터리 생성

FROM node:18-alpine

